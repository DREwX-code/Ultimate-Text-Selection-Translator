// ==UserScript==
// @name             Ultimate Text Selection Translator – Instantly Translate Any Selected Text
// @name:fr          Ultimate Text Selection Translator – Traduisez instantanément n'importe quel texte sélectionné
// @name:es          Ultimate Text Selection Translator – Traduce instantáneamente cualquier texto seleccionado
// @name:de          Ultimate Text Selection Translator – Übersetzen Sie jeden ausgewählten Text sofort
// @name:ru          Ultimate Text Selection Translator – Мгновенно переводите любой выделенный текст
// @name:zh-CN       Ultimate Text Selection Translator – 立即翻译任何选定的文本
// @name:zh-TW       Ultimate Text Selection Translator – 立即翻譯任何選定的文本
// @name:ja          Ultimate Text Selection Translator – 選択したテキストを即座に翻訳
// @name:pt          Ultimate Text Selection Translator – Traduza instantaneamente qualquer texto selecionado
// @name:it          Ultimate Text Selection Translator – Traduci istantaneamente qualsiasi testo selezionato
// @name:ar          Ultimate Text Selection Translator – ترجمة فورية لأي نص محدد
// @name:be          Ultimate Text Selection Translator – Імгненна перакладайце любы выбраны тэкст
// @name:bg          Ultimate Text Selection Translator – Незабавен превод на всеки избран текст
// @name:cs          Ultimate Text Selection Translator – Okamžitě přeložte jakýkoli vybraný text
// @name:da          Ultimate Text Selection Translator – Oversæt øjeblikkeligt enhver valgt tekst
// @name:el          Ultimate Text Selection Translator – Μεταφράστε άμεσα οποιοδήποτε επιλεγμένο κείμενο
// @name:eo          Ultimate Text Selection Translator – Tuj Traduku Iun Elektitan Tekston
// @name:fi          Ultimate Text Selection Translator – Käännä välittömästi kaikki valitut tekstit
// @name:he          Ultimate Text Selection Translator – תרגם באופן מיידי כל טקסט שנבחר
// @name:hr          Ultimate Text Selection Translator – Trenutačno prevedite bilo koji odabrani tekst
// @name:hu          Ultimate Text Selection Translator – Azonnal lefordíthatja a kiválasztott szöveget
// @name:id          Ultimate Text Selection Translator – Terjemahkan Teks yang Dipilih Secara Instan
// @name:ka          Ultimate Text Selection Translator – მყისიერად თარგმნეთ ნებისმიერი არჩეული ტექსტი
// @name:ko          Ultimate Text Selection Translator – 선택한 텍스트를 즉시 번역하세요
// @name:mr          Ultimate Text Selection Translator – कोणताही निवडलेला मजकूर त्वरित अनुवादित करा
// @name:nl          Ultimate Text Selection Translator – Vertaal onmiddellijk elke geselecteerde tekst
// @name:nb          Ultimate Text Selection Translator – Oversett alle valgt tekst umiddelbart
// @name:pl          Ultimate Text Selection Translator – Natychmiast przetłumacz dowolny zaznaczony tekst
// @name:pt-BR       Ultimate Text Selection Translator – Traduza instantaneamente qualquer texto selecionado
// @name:ro          Ultimate Text Selection Translator – Traduceți instantaneu orice text selectat
// @name:sk          Ultimate Text Selection Translator – Okamžite preložte akýkoľvek vybraný text
// @name:sr          Ultimate Text Selection Translator – Одмах преведите било који одабрани текст
// @name:sv          Ultimate Text Selection Translator – Översätt direkt valfri text
// @name:th          Ultimate Text Selection Translator – แปลข้อความที่เลือกทันที
// @name:tr          Ultimate Text Selection Translator – Seçilen Metni Anında Çevir
// @name:ug          Ultimate Text Selection Translator – تاللانغان تېكىستنى دەرھال تەرجىمە قىلىڭ
// @name:uk          Ultimate Text Selection Translator – Миттєво перекладіть будь-який виділений текст
// @name:vi          Ultimate Text Selection Translator – Dịch ngay lập tức mọi văn bản đã chọn
// @name:fr-CA       Ultimate Text Selection Translator – Traduisez instantanément n'importe quel texte sélectionné
// @name:ckb         Ultimate Text Selection Translator – Her Nivîsarek Hilbijartî tavilê Wergerîne
// @name:es-419      Ultimate Text Selection Translator – Traduce instantáneamente cualquier texto seleccionado

// @description       Instantly translate selected text using the smart button or the Ctrl+L shortcut. Automatically detects the language and translates it into the language of your choice.
// @description:fr    Traduisez instantanément le texte sélectionné grâce au bouton intelligent ou au raccourci Ctrl+L. Détection automatique de la langue et traduction immédiate dans la langue de votre choix.
// @description:es    Traduce instantáneamente el texto seleccionado mediante el botón inteligente o el atajo Ctrl+L. Detecta automáticamente el idioma y lo traduce al idioma de tu elección.
// @description:de    Übersetzen Sie ausgewählten Text sofort über die intelligente Schaltfläche oder die Tastenkombination Strg+L. Erkennt die Sprache automatisch und übersetzt sie in die Sprache Ihrer Wahl.
// @description:ru    Мгновенно переводите выделенный текст с помощью умной кнопки или сочетания Ctrl+L. Автоматически определяет язык и переводит его на выбранный вами язык.
// @description:zh-CN 使用智能按钮或 Ctrl+L 快捷键立即翻译所选文本。自动检测语言并将其翻译为您选择的语言。
// @description:zh-TW 使用智慧按鈕或 Ctrl+L 快捷鍵立即翻譯所選文字。自動偵測語言並翻譯為您選擇的語言。
// @description:ja    スマートボタンまたは Ctrl+L ショートカットで選択したテキストを即座に翻訳します。言語を自動検出し、選択した言語に翻訳します。
// @description:pt    Traduza instantaneamente o texto selecionado usando o botão inteligente ou o atalho Ctrl+L. Detecta automaticamente o idioma e o traduz para o idioma de sua escolha.
// @description:it    Traduci istantaneamente il testo selezionato tramite il pulsante intelligente o la scorciatoia Ctrl+L. Rileva automaticamente la lingua e la traduce nella lingua scelta.
// @description:ar    ترجم النص المحدد فورًا باستخدام الزر الذكي أو اختصار Ctrl+L. يكتشف اللغة تلقائيًا ويترجمها إلى اللغة التي تختارها.
// @description:be    Імгненна перакладайце вылучаны тэкст з дапамогай разумнай кнопкі або спалучэння Ctrl+L. Аўтаматычна вызначае мову і перакладае яе на мову па вашым выбары.
// @description:bg    Превеждайте избрания текст незабавно чрез интелигентния бутон или клавишната комбинация Ctrl+L. Автоматично разпознава езика и го превежда на езика по ваш избор.
// @description:cs    Okamžitě přeložte vybraný text pomocí chytrého tlačítka nebo zkratky Ctrl+L. Automaticky rozpozná jazyk a přeloží jej do vámi zvoleného jazyka.
// @description:da    Oversæt valgt tekst med det samme ved hjælp af den smarte knap eller genvejen Ctrl+L. Registrerer automatisk sproget og oversætter det til det sprog, du vælger.
// @description:el    Μεταφράστε άμεσα το επιλεγμένο κείμενο χρησιμοποιώντας το έξυπνο κουμπί ή τη συντόμευση Ctrl+L. Ανιχνεύει αυτόματα τη γλώσσα και τη μεταφράζει στη γλώσσα της επιλογής σας.
// @description:eo    Traduku elektitan tekston tuj per la inteligenta butono aŭ la klavkombino Ctrl+L. Aŭtomate detektas la lingvon kaj tradukas ĝin al la lingvo laŭ via elekto.
// @description:fi    Käännä valittu teksti välittömästi älypainikkeella tai Ctrl+L-pikanäppäimellä. Tunnistaa kielen automaattisesti ja kääntää sen valitsemaasi kieleen.
// @description:he    תרגם טקסט נבחר באופן מיידי באמצעות הכפתור החכם או קיצור הדרך Ctrl+L. מזהה אוטומטית את השפה ומתרגם אותה לשפה שתבחר.
// @description:hr    Odmah prevedite odabrani tekst pomoću pametnog gumba ili prečaca Ctrl+L. Automatski prepoznaje jezik i prevodi ga na jezik po vašem izboru.
// @description:hu    Fordítsa le azonnal a kijelölt szöveget az intelligens gombbal vagy a Ctrl+L billentyűkombinációval. Automatikusan felismeri a nyelvet, és az Ön által választott nyelvre fordítja.
// @description:id    Terjemahkan teks yang dipilih secara instan menggunakan tombol pintar atau pintasan Ctrl+L. Secara otomatis mendeteksi bahasa dan menerjemahkannya ke bahasa pilihan Anda.
// @description:ka    შერჩეული ტექსტი მყისიერად თარგმნეთ ჭკვიანი ღილაკის ან Ctrl+L მალსახმობის გამოყენებით. ავტომატურად ამოიცნობს ენას და თარგმნის თქვენს მიერ არჩეულ ენაზე.
// @description:ko    스마트 버튼 또는 Ctrl+L 단축키를 사용하여 선택한 텍스트를 즉시 번역하세요. 언어를 자동으로 감지하고 원하는 언어로 번역합니다.
// @description:mr    स्मार्ट बटण किंवा Ctrl+L शॉर्टकट वापरून निवडलेला मजकूर त्वरित भाषांतरित करा. भाषा आपोआप ओळखते आणि तुमच्या निवडीच्या भाषेत अनुवादित करते.
// @description:nl    Vertaal geselecteerde tekst direct met de slimme knop of de sneltoets Ctrl+L. Detecteert automatisch de taal en vertaalt deze naar de taal van jouw keuze.
// @description:nb    Oversett valgt tekst umiddelbart med den smarte knappen eller Ctrl+L-snarveien. Oppdager språket automatisk og oversetter det til språket du velger.
// @description:pl    Natychmiast przetłumacz zaznaczony tekst za pomocą inteligentnego przycisku lub skrótu Ctrl+L. Automatycznie wykrywa język i tłumaczy go na wybrany przez Ciebie język.
// @description:pt-BR Traduza instantaneamente o texto selecionado usando o botão inteligente ou o atalho Ctrl+L. Detecta automaticamente o idioma e o traduz para o idioma de sua escolha.
// @description:ro    Traduceți instantaneu textul selectat folosind butonul inteligent sau combinația Ctrl+L. Detectează automat limba și o traduce în limba aleasă de dvs.
// @description:sk    Okamžite preložte vybraný text pomocou inteligentného tlačidla alebo skratky Ctrl+L. Automaticky rozpozná jazyk a preloží ho do jazyka podľa vášho výberu.
// @description:sr    Одмах преведите изабрани текст помоћу паметног дугмета или пречице Ctrl+L. Аутоматски препознаје језик и преводи га на језик по вашем избору.
// @description:sv    Översätt markerad text direkt med den smarta knappen eller genvägen Ctrl+L. Identifierar automatiskt språket och översätter det till det språk du väljer.
// @description:th    แปลข้อความที่เลือกทันทีด้วยปุ่มอัจฉริยะหรือคีย์ลัด Ctrl+L ระบบจะตรวจจับภาษาอัตโนมัติและแปลเป็นภาษาที่คุณเลือก
// @description:tr    Seçilen metni akıllı düğme veya Ctrl+L kısayolu ile anında çevirin. Dili otomatik olarak algılar ve seçtiğiniz dile çevirir.
// @description:ug    تاللانغان تېكىستنى ئەقلىي كۇنۇپكا ياكى Ctrl+L قىسقا يولى ئارقىلىق دەرھال تەرجىمە قىلىڭ. تىلنى ئاپتوماتىك بايقىپ، سىز تاللىغان تىلغا تەرجىمە قىلىدۇ.
// @description:uk    Миттєво перекладіть виділений текст за допомогою розумної кнопки або поєднання Ctrl+L. Автоматично визначає мову та перекладає її на обрану вами мову.
// @description:vi    Dịch ngay văn bản đã chọn bằng nút thông minh hoặc phím tắt Ctrl+L. Tự động phát hiện ngôn ngữ và dịch sang ngôn ngữ bạn chọn.
// @description:fr-CA Traduisez instantanément le texte sélectionné grâce au bouton intelligent ou au raccourci Ctrl+L. Détection automatique de la langue et traduction immédiate dans la langue de votre choix.
// @description:ckb   دەقە هەڵبژێردراوەکانت بە شێوەیەکی خێرا بە دوگمەی زیرەک یان Ctrl+L وەرگێڕە. زمان بە ئۆتۆماتیکی دەدۆزێتەوە و دەیگۆڕێتە سەر زمانی هەڵبژێردراوی تۆ.
// @description:es-419 Traduce instantáneamente el texto seleccionado mediante el botón inteligente o el atajo Ctrl+L. Detecta automáticamente el idioma y lo traduce al idioma que elijas.

// @namespace    https://github.com/DREwX-code
// @author       Dℝ∃wX
// @copyright    2025-2026 Dℝ∃wX
// @license      Apache-2.0
// @require      https://update.greasyfork.org/scripts/556911/1864194/UTST%20Translation%20Library.js
// @grant        GM_xmlhttpRequest
// @grant        GM_getValue
// @grant        GM_setValue
// @grant        GM_addStyle
// @connect      translate.googleapis.com
// @match        *://*/*
// @noframes
// @run-at       document-start
// @version      1.4.5
// @icon         https://raw.githubusercontent.com/DREwX-code/Ultimate-Text-Selection-Translator/refs/heads/main/assets/icons/Icon_Translate_Script.png
// @tag          translation
// @tag          text selection
// @tag          translate
// @tag          google translate
// @tag          shortcut
// @tag          productivity
// @tag          accessibility
// @tag          language
// @tag          multilingual

// ==/UserScript==

/*
Copyright 2025-2026 Dℝ∃wX

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    https://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
*/

import { createLanguageModel } from './language/model.js';
import { createSpeechService } from './speech-service.js';
import { createLayoutController } from './layout-controller.js';
import { createShortcutController } from './shortcuts/controller.js';
import { createSelectionController } from './selection-controller.js';
import { createLanguagePanels } from './language/panels.js';
import { createThemeController } from './theme-controller.js';
import { createPanelController } from './panel-controller.js';
import { createFullscreenController } from './fullscreen-controller.js';
import { createSettingsController } from './settings-controller.js';
import {
    createBootstrapRuntime,
    UTST_LOGO_URL
} from './bootstrap-runtime.js';
import {
    getShadowSafeStyleText,
    setImportantStyle,
    UTST_STYLE_TEXT
} from './ui-styles.js';
import {
    cloneDefaultShortcut,
    loadBubbleBlacklist,
    loadDefaultTargetLanguage as loadStoredDefaultTargetLanguage,
    loadPanelTheme,
    loadSelectionBubbleEnabled,
    loadShortcutSetting,
    loadToolLanguagePreference,
    normalizePanelTheme,
    saveBubbleBlacklist,
    saveDefaultTargetLanguage as saveStoredDefaultTargetLanguage,
    saveNormalizedShortcutSetting,
    savePanelTheme,
    saveSelectionBubbleEnabled,
    saveShortcutSetting as persistShortcutSetting
} from './storage.js';
import { createTranslationService } from './translation-service.js';
import {
    createFullscreenOverlayView,
    createSelectionBubbleView,
    createTranslationBoxView,
    createUiRefs
} from './ui-views.js';
import { getLocalizedValue } from './utils.js';


function runUltimateTextSelectionTranslator() {
    'use strict';

    const bootstrapRuntime = createBootstrapRuntime({
        windowRef: typeof window !== 'undefined' ? window : null,
        documentRef: document,
        globalRef: typeof globalThis !== 'undefined' ? globalThis : null,
        getImageConstructor: () => typeof Image === 'function' ? Image : null,
        logoUrl: UTST_LOGO_URL,
        addStyle: cssText => GM_addStyle(cssText),
        getShadowSafeStyleText,
        setImportantStyle,
        makeEvent: type => new CustomEvent(type)
    });
    const {
        connectLogoHydration,
        createIsolatedUiRoot,
        eventPathContains,
        getTranslationLibrary,
        scheduleUtstLogoPreload
    } = bootstrapRuntime;

    scheduleUtstLogoPreload();

    function bootstrap() {
        const utstUi = createIsolatedUiRoot(UTST_STYLE_TEXT);
        const utstUiRoot = utstUi.root;

        const translationLibrary = getTranslationLibrary();
        if (!translationLibrary || !translationLibrary.languageNames) {
            console.error('[Ultimate Translator] Missing TraductionOutilTranslator language library.');
            return;
        }

        const browserLang = navigator.language.split('-')[0];
        const languageModel = createLanguageModel({ translationLibrary, browserLang });
        const {
            googleTranslateLanguages,
            languageNames,
            resolveUiLang,
            supportedUiLanguages
        } = languageModel;

        const normalizedToolLangPref = loadToolLanguagePreference(supportedUiLanguages);

        let toolLanguagePreference = normalizedToolLangPref;
        const initialLanguageState = languageModel.getLocalizedRuntimeState(toolLanguagePreference);
        let {
            langNames,
            errors,
            tooltips,
            dragHandleLabel,
            overlayLabels,
            settingsTitle,
            settingsDefaultLabel,
            settingsToolLabel
        } = initialLanguageState;

        const defaultTargetLang = languageModel.getDefaultTargetLanguage(langNames);

        function getLanguageLabel(code) {
            return languageModel.getLanguageLabel(code, langNames);
        }

        function buildTargetLanguageOptions(includeNavigator = false) {
            return languageModel.buildTargetLanguageOptions(includeNavigator, langNames);
        }

        function buildToolLanguageOptionsHtml() {
            return languageModel.buildToolLanguageOptionsHtml();
        }

        function buildSourceLanguageOptionsHtml() {
            return languageModel.buildSourceLanguageOptionsHtml(langNames);
        }

        const toolLanguageOptionsHtml = buildToolLanguageOptionsHtml();
        let sourceLanguageOptionsHtml = buildSourceLanguageOptionsHtml();

        const targetLanguageOptionsHtml = buildTargetLanguageOptions(true);

        // Phase 1: create the views and collect their DOM references.
        const translationBox = createTranslationBoxView({
            documentRef: document,
            root: utstUiRoot,
            logoUrl: UTST_LOGO_URL,
            dragHandleLabel,
            settingsTitle,
            googleTranslateLanguages,
            targetLanguageOptionsHtml,
            overlayLabels,
            tooltips,
            settingsDefaultLabel,
            settingsToolLabel,
            toolLanguageOptionsHtml,
            langNames,
            shortcutLabel: getLocalizedValue(langNames, languageNames.en, 'settingsShortcutLabel'),
            shortcutResetLabel: getLocalizedValue(langNames, languageNames.en, 'settingsShortcutReset')
        });

        const fullscreenOverlay = createFullscreenOverlayView({
            documentRef: document,
            root: utstUiRoot,
            logoUrl: UTST_LOGO_URL,
            overlayLabels,
            langNames,
            sourceLanguageOptionsHtml,
            targetLanguageOptionsHtml,
            defaultTargetLabel: getLanguageLabel(defaultTargetLang)
        });

        connectLogoHydration(utstUiRoot);

        const selectionBubble = createSelectionBubbleView({
            documentRef: document,
            root: utstUiRoot,
            bubbleLabels: langNames.bubble
        });

        const uiRefs = createUiRefs({
            root: utstUiRoot,
            host: utstUi.host,
            translationBox,
            fullscreenOverlay,
            selectionBubble
        });
        const {
            closeButton: selectionBubbleClose,
            actionButton: selectionBubbleAction,
            closeMenu: bubbleCloseMenu,
            hideSiteButton: bubbleHideSiteButton,
            hideGlobalButton: bubbleHideGlobalButton
        } = uiRefs.selection;
        const {
            sourceLangSelect,
            targetLangSelect,
            translationText,
            loadingOverlay: panelLoadingOverlay,
            loadingTitle: panelLoadingTitle,
            speakButton,
            speakTooltip,
            speakTranslated,
            speakOriginal,
            copyButton,
            settingsButton,
            backButton,
            closeButton,
            sourceAutoOption,
            translatorPanel,
            settingsPanel,
            settingsHeader,
            settingsHeaderTitle,
            fullscreenToggle,
            dragHandle
        } = uiRefs.panel;
        const {
            defaultTargetSelect: defaultTranslateLangSelect,
            toolLanguageSelect,
            themeSelect: panelThemeSelect,
            themeTrigger: panelThemeTrigger,
            themeCurrent: panelThemeCurrent,
            themePanel: panelThemePanel,
            themeGrid: panelThemeGrid,
            selectionBubbleEnabledCheckbox,
            bubbleBlacklistInput,
            bubbleBlacklistAddButton,
            bubbleBlacklistList,
            defaultTargetLabel: defaultTranslateLangLabel,
            toolLanguageLabel,
            themeLabel: panelThemeLabel,
            shortcutCaptureLabel,
            shortcutCaptureButton,
            shortcutResetButton,
            shortcutCaptureHelp,
            bubbleToggleLabel,
            bubbleBlacklistLabel
        } = uiRefs.settings;
        const {
            title: fullscreenTitleEl,
            closeButton: fullscreenClose,
            sourceLangSelect: fullscreenSourceLangSelect,
            targetLangSelect: fullscreenTargetLangSelect,
            sourceLangCurrent: fullscreenSourceLangCurrent,
            targetLangCurrent: fullscreenTargetLangCurrent,
            sourceLangSearch: fullscreenSourceLangSearch,
            targetLangSearch: fullscreenTargetLangSearch,
            sourceLangGrid: fullscreenSourceLangGrid,
            targetLangGrid: fullscreenTargetLangGrid,
            sourceLangPanel: fullscreenSourceLangPanel,
            targetLangPanel: fullscreenTargetLangPanel,
            sourceLangTrigger: fullscreenSourceLangTrigger,
            targetLangTrigger: fullscreenTargetLangTrigger,
            panel: fullscreenPanel,
            sourceLabel: fullscreenSourceLabel,
            targetLabel: fullscreenTargetLabel,
            swapButton: fullscreenSwap,
            sourceText: fullscreenSource,
            targetText: fullscreenTarget,
            sourceWrap: fullscreenSourceWrap,
            targetWrap: fullscreenTargetWrap,
            loadingOverlay: fullscreenLoadingOverlay,
            loadingTitle: fullscreenLoadingTitle,
            sourceCopyButton: fullscreenSourceCopy,
            sourceSpeakButton: fullscreenSourceSpeak,
            targetCopyButton: fullscreenTargetCopy,
            targetSpeakButton: fullscreenTargetSpeak
        } = uiRefs.fullscreen;
        if (panelThemePanel) {
            panelThemePanel.classList.add('utst-inline-lang-panel');
            utstUiRoot.appendChild(panelThemePanel);
        }

        // Phase 2: create services, controllers and their targeted facades.
        const layoutController = createLayoutController({
            windowRef: window,
            documentRef: document,
            translationBox,
            dragHandle,
            fullscreenOverlay,
            fullscreenSource,
            fullscreenTarget,
            fullscreenSourceWrap,
            fullscreenTargetWrap,
            requestFrame: (callback) => requestAnimationFrame(callback),
            cancelFrame: (frameId) => cancelAnimationFrame(frameId)
        });
        const {
            MARGIN,
            finishFullscreenTextareaResize,
            lockPageScrollForFullscreen,
            markFullscreenResizeStart,
            placeBoxAtSelection,
            resetFullscreenTextareaResize,
            syncFullscreenTextareaHeights,
            unlockPageScrollForFullscreen
        } = layoutController;

        sourceLangSelect.value = 'auto';

        let currentSelectedText = '';
        let currentTranslatedText = '';
        let detectedSourceLang = 'auto';
        let currentResolvedTargetLang = browserLang;
        const selectionController = createSelectionController({
            windowRef: window,
            documentRef: document,
            selectionBubble,
            selectionBubbleClose,
            selectionBubbleAction,
            bubbleCloseMenu,
            bubbleHideSiteButton,
            bubbleHideGlobalButton,
            translationBox,
            fullscreenOverlay,
            selectionBubbleEnabledCheckbox,
            bubbleBlacklistInput,
            bubbleBlacklistAddButton,
            bubbleBlacklistList,
            margin: MARGIN,
            eventPathContains,
            loadSelectionBubbleEnabled,
            loadBubbleBlacklist,
            saveSelectionBubbleEnabled,
            saveBubbleBlacklist,
            getLanguageState: () => ({
                langNames,
                fallback: languageNames.en || {}
            }),
            onTranslateSelection: (text, position) => {
                openTranslationPanelForText(text, position);
            },
            setTimer: (callback, delay) => setTimeout(callback, delay),
            clearTimer: (timerId) => clearTimeout(timerId)
        });
        const {
            bindSelectionBubbleControls,
            bindSelectionEvents,
            getSelectionContext,
            hideBubbleCloseMenu,
            hideSelectionBubble,
            isFullscreenOpen,
            scheduleSelectionBubbleUpdate,
            syncSelectionBubbleSettingsUi
        } = selectionController;
        let themeController;
        let fullscreenController;
        let speechStateReady = false;
        const languagePanels = createLanguagePanels({
            windowRef: window,
            documentRef: document,
            root: utstUiRoot,
            fullscreenSourceLangSelect,
            fullscreenTargetLangSelect,
            fullscreenSourceLangCurrent,
            fullscreenTargetLangCurrent,
            fullscreenSourceLangSearch,
            fullscreenTargetLangSearch,
            fullscreenSourceLangGrid,
            fullscreenTargetLangGrid,
            fullscreenSourceLangPanel,
            fullscreenTargetLangPanel,
            fullscreenSourceLangTrigger,
            fullscreenTargetLangTrigger,
            getDefaultTargetLanguage: () => defaultTargetLang,
            getLanguageLabel,
            getNavigatorPlaceholder: () => langNames.navigator,
            getPanelThemeStyles: () => themeController.getLanguagePanelThemeStyles(),
            applyLanguageGridSelection: (code, selectEl) => getFullscreenController().applyLanguageGridSelection(code, selectEl),
            updateLanguageGridCurrentLabel: (currentLabelEl, code) => getFullscreenController().updateLanguageGridCurrentLabel(currentLabelEl, code),
            onFullscreenLanguageSelection: () => {
                getFullscreenController().scheduleFullscreenTranslate(0, 'language');
            },
            eventPathContains,
            createChangeEvent: () => new Event('change', { bubbles: true })
        });
        const {
            attachInlineLanguagePanel,
            bindFullscreenLanguageSearchControls,
            bindFullscreenPanelTriggers,
            closeLanguagePanelsFromOutside,
            hideInlinePanels,
            hideLanguagePanels,
            isClickInFullscreenLanguagePanel,
            isClickInInlineLanguagePanel,
            refreshInlineLanguagePlaceholders,
            refreshLanguagePanelsTheme,
            renderLanguageGrid,
            updateInlinePanelsPosition: updateLanguagePanelsPosition
        } = languagePanels;
        themeController = createThemeController({
            windowRef: window,
            documentRef: document,
            host: utstUi.host,
            translationBox,
            fullscreenOverlay,
            speakButton,
            copyButton,
            fullscreenToggle,
            settingsButton,
            backButton,
            fullscreenSwap,
            fullscreenSourceCopy,
            fullscreenSourceSpeak,
            fullscreenTargetCopy,
            fullscreenTargetSpeak,
            panelThemeSelect,
            panelThemeTrigger,
            panelThemeCurrent,
            panelThemePanel,
            panelThemeGrid,
            normalizePanelTheme,
            loadPanelTheme,
            savePanelTheme,
            getLocalizedThemes: () => langNames.themes || languageNames.en.themes || {},
            refreshLanguagePanelsTheme,
            hideInlinePanels,
            hideLanguagePanels,
            eventPathContains,
            isSpeechStateReady: () => speechStateReady,
            refreshSpeechIconState: () => updateSpeechIconState()
        });
        const {
            COPY_FEEDBACK_STROKE,
            applyCurrentPanelTheme,
            applyIconThemeColors,
            bindThemeControls,
            closeThemePanelFromOutside,
            getIconDefaultStrokeColor,
            positionThemePanel,
            refreshLanguagePanelTheme,
            refreshThemeOptionsLabels,
            setButtonIconStroke
        } = themeController;
        const panelLanguageApi = {
            getBrowserLanguage: () => browserLang,
            getErrors: () => errors,
            getPanelTexts: () => ({
                autoLabel: langNames.auto,
                dragHandleLabel,
                listenTranslated: tooltips.listenTranslated,
                listenOriginal: tooltips.listenOriginal
            }),
            getSettingsTitle: () => settingsTitle,
            getLoaderTitleByMode,
            getSavedTargetLanguage,
            ensureSelectValue,
            resolveSourceSpeechLanguage,
            resolveTargetSpeechLanguage
        };
        const translationApi = { translateText };
        const panelSpeechApi = {
            stopSpeaking,
            speak,
            getSpeechState: () => ({
                playing: speechPlaying,
                speakerId: currentSpeakerId
            })
        };
        const panelLayoutApi = { placeBoxAtSelection };
        const panelSelectionApi = {
            hideSelectionBubble,
            hideBubbleCloseMenu,
            scheduleSelectionBubbleUpdate
        };
        const panelThemeApi = {
            setButtonIconStroke,
            copyFeedbackStroke: COPY_FEEDBACK_STROKE,
            applyIconThemeColors
        };
        const panelRuntimeState = {
            getCurrentSelectedText: () => currentSelectedText,
            setCurrentSelectedText: text => {
                currentSelectedText = text;
            },
            getCurrentTranslatedText: () => currentTranslatedText,
            setCurrentTranslatedText: text => {
                currentTranslatedText = text;
            },
            getCurrentResolvedTargetLanguage: () => currentResolvedTargetLang,
            setCurrentResolvedTargetLanguage: language => {
                currentResolvedTargetLang = language;
            },
            setDetectedSourceLanguage: language => {
                detectedSourceLang = language;
            }
        };
        const panelController = createPanelController({
            windowRef: window,
            documentRef: document,
            ui: {
                translationBox,
                sourceLangSelect,
                targetLangSelect,
                defaultTranslateLangSelect,
                translationText,
                panelLoadingOverlay,
                panelLoadingTitle,
                speakButton,
                speakTooltip,
                speakTranslated,
                speakOriginal,
                copyButton,
                settingsButton,
                backButton,
                closeButton,
                sourceAutoOption,
                translatorPanel,
                settingsPanel,
                settingsHeader,
                settingsHeaderTitle
            },
            languageApi: panelLanguageApi,
            translationApi,
            speechApi: panelSpeechApi,
            layoutApi: panelLayoutApi,
            selectionApi: panelSelectionApi,
            themeApi: panelThemeApi,
            runtimeState: panelRuntimeState,
            setLoaderState,
            eventPathContains,
            writeClipboardText: text => navigator.clipboard.writeText(text),
            setTimer: (callback, delay) => setTimeout(callback, delay)
        });
        const {
            bindPanelActionControls,
            bindPanelNavigationControls,
            bindPanelOutsideControls,
            handleLanguageChange,
            openTranslationPanelForText,
            syncPanelLoadingTitle,
            updateNoTextErrorMessage,
            updateTranslatorTexts
        } = panelController;
        const shortcutController = createShortcutController({
            documentRef: document,
            navigatorRef: navigator,
            shortcutCaptureLabel,
            shortcutCaptureButton,
            shortcutResetButton,
            shortcutCaptureHelp,
            cloneDefaultShortcut,
            loadShortcutSetting,
            persistShortcutSetting,
            saveNormalizedShortcutSetting,
            getLanguageState: () => ({
                langNames,
                fallback: languageNames.en || {}
            }),
            getUiLanguage: () => resolveUiLang(toolLanguagePreference),
            onShortcutTriggered: () => {
                const context = getSelectionContext();
                openTranslationPanelForText(context ? context.text : '', context ? context.position : null);
            }
        });
        const {
            bindShortcutControls,
            bindGlobalShortcut,
            refreshKeyboardLayoutMap,
            updateShortcutSettingsUi
        } = shortcutController;

        function resolveTargetLanguageValue(value, fallback = defaultTargetLang) {
            let lang = value || fallback;
            if (lang === 'navigator') return browserLang;
            if (!lang || lang === 'auto') lang = fallback;
            if (lang === 'navigator') return browserLang;
            return lang || browserLang;
        }

        function resolveSourceSpeechLanguage(sourceValue) {
            if (sourceValue && sourceValue !== 'auto') return sourceValue;
            if (detectedSourceLang && detectedSourceLang !== 'auto') return detectedSourceLang;
            if (sourceLangSelect && sourceLangSelect.value && sourceLangSelect.value !== 'auto') return sourceLangSelect.value;
            return browserLang;
        }

        function resolveTargetSpeechLanguage(targetValue, fallback = currentResolvedTargetLang) {
            return resolveTargetLanguageValue(targetValue, fallback || browserLang);
        }

        function getLoaderTitleByMode(mode = 'translate') {
            const translateLabel = (overlayLabels && overlayLabels.translate) || (langNames.overlay && langNames.overlay.translate) || 'Translate';
            if (mode === 'language') {
                return `${translateLabel}...`;
            }
            return `${translateLabel}...`;
        }

        function setLoaderState(loaderEl, titleEl, active, mode = 'translate') {
            if (!loaderEl) return;
            loaderEl.dataset.mode = mode === 'language' ? 'language' : 'translate';
            loaderEl.classList.toggle('is-active', !!active);
            loaderEl.setAttribute('aria-hidden', active ? 'false' : 'true');
            if (titleEl) {
                titleEl.textContent = getLoaderTitleByMode(loaderEl.dataset.mode);
            }
        }

        function ensureSelectValue(selectEl, lang) {
            if (selectEl.querySelector(`option[value="${lang}"]`)) {
                selectEl.value = lang;
                return lang;
            }
            selectEl.value = defaultTargetLang;
            return defaultTargetLang;
        }

        function getSavedTargetLanguage() {
            return loadStoredDefaultTargetLanguage(
                defaultTargetLang,
                language => !!targetLangSelect.querySelector(`option[value="${language}"]`)
            );
        }

        function persistDefaultTargetLanguage(lang) {
            return saveStoredDefaultTargetLanguage(
                lang,
                defaultTargetLang,
                language => !!defaultTranslateLangSelect.querySelector(`option[value="${language}"]`)
            );
        }

        const settingsController = createSettingsController({
            ui: {
                defaultTranslateLangSelect,
                toolLanguageSelect,
                targetLangSelect,
                defaultTranslateLangLabel,
                toolLanguageLabel,
                panelThemeLabel,
                bubbleToggleLabel,
                bubbleBlacklistLabel,
                bubbleBlacklistAddButton,
                settingsButton,
                settingsHeaderTitle
            },
            languageApi: {
                getBrowserLanguage: () => browserLang,
                getDefaultTargetLanguage: () => defaultTargetLang,
                getLanguageNames: () => langNames,
                getEnglishLanguageNames: () => languageNames.en,
                getErrors: () => errors,
                getLocalizedRuntimeState: preference => languageModel.getLocalizedRuntimeState(preference),
                getSupportedUiLanguages: () => supportedUiLanguages,
                getSavedTargetLanguage,
                persistDefaultTargetLanguage,
                getSettingsTextState: () => ({
                    settingsTitle,
                    settingsDefaultLabel,
                    settingsToolLabel
                }),
                buildToolLanguageOptionsHtml,
                buildTargetLanguageOptions,
                ensureSelectValue
            },
            runtimeState: {
                getToolLanguagePreference: () => toolLanguagePreference,
                setToolLanguagePreference: preference => {
                    toolLanguagePreference = preference;
                },
                setLocalizedRuntimeStateValues: localizedState => {
                    langNames = localizedState.langNames;
                    errors = localizedState.errors;
                    tooltips = localizedState.tooltips;
                    dragHandleLabel = localizedState.dragHandleLabel;
                    overlayLabels = localizedState.overlayLabels;
                    settingsTitle = localizedState.settingsTitle;
                    settingsDefaultLabel = localizedState.settingsDefaultLabel;
                    settingsToolLabel = localizedState.settingsToolLabel;
                },
                setCurrentResolvedTargetLanguage: language => {
                    currentResolvedTargetLang = language;
                }
            },
            speechApi: { stopSpeaking },
            panelApi: {
                handleLanguageChange,
                updateTranslatorTexts,
                updateNoTextErrorMessage
            },
            fullscreenApi: {
                updateFullscreenTexts: () => getFullscreenController().updateFullscreenTexts(),
                refreshFullscreenLanguageSelects: () => getFullscreenController().refreshFullscreenLanguageSelects()
            },
            themeApi: {
                refreshThemeOptionsLabels,
                bindThemeControls,
                applyCurrentPanelTheme
            },
            shortcutApi: {
                updateShortcutSettingsUi,
                bindShortcutControls,
                refreshKeyboardLayoutMap
            },
            languagePanelsApi: { refreshInlineLanguagePlaceholders },
            selectionApi: {
                syncSelectionBubbleSettingsUi,
                scheduleSelectionBubbleUpdate,
                bindSelectionBubbleControls
            }
        });
        const {
            initializeSettingsControls
        } = settingsController;

        const translationService = createTranslationService({
            getErrorMessages: () => errors,
            isSupportedDetectedLanguage: detected => Boolean(googleTranslateLanguages[detected]),
            onDetectedLanguage: runDetectedLang => {
                if (runDetectedLang && sourceLangSelect.querySelector(`option[value="${runDetectedLang}"]`)) {
                    sourceLangSelect.value = runDetectedLang;
                    detectedSourceLang = runDetectedLang;
                } else {
                    sourceLangSelect.value = 'auto';
                    detectedSourceLang = 'auto';
                }
                getFullscreenController().updateFullscreenSourceCurrentLabel();
            }
        });

        function translateText(text, sourceLang, targetLang, callback, position) {
            if (!text) {
                translationService.translateText(text, sourceLang, defaultTargetLang, (translation, resolvedTargetLang) => {
                    callback(translation, position, resolvedTargetLang);
                });
                return;
            }

            let resolvedTargetLang = targetLang;
            if (resolvedTargetLang === 'navigator') {
                resolvedTargetLang = browserLang;
            }
            if (!resolvedTargetLang || resolvedTargetLang === '') {
                let fallback = getSavedTargetLanguage();
                if (fallback === 'navigator') fallback = browserLang;
                resolvedTargetLang = fallback || defaultTargetLang;
            }

            translationService.translateText(text, sourceLang, resolvedTargetLang, (translation, serviceTargetLang) => {
                callback(translation, position, serviceTargetLang);
            });
        }


        let currentSpeakerId = null;
        let speechPlaying = false;
        const SPEECH_ACTIVE_STROKE = COPY_FEEDBACK_STROKE;

        function setSpeakButtonVisualState(buttonEl, active) {
            if (!buttonEl) return;
            setButtonIconStroke(buttonEl, active ? SPEECH_ACTIVE_STROKE : getIconDefaultStrokeColor());
        }

        function updateSpeechIconState() {
            const panelActive = speechPlaying && currentSpeakerId && currentSpeakerId.startsWith('panel-');
            const sourceActive = speechPlaying && currentSpeakerId === 'fs-source';
            const targetActive = speechPlaying && currentSpeakerId === 'fs-target';

            setSpeakButtonVisualState(speakButton, panelActive);
            setSpeakButtonVisualState(fullscreenSourceSpeak, sourceActive);
            setSpeakButtonVisualState(fullscreenTargetSpeak, targetActive);
        }

        const speechService = createSpeechService({
            browserLanguage: browserLang,
            onStateChange: ({ playing, speakerId }) => {
                speechPlaying = playing;
                currentSpeakerId = speakerId;
                updateSpeechIconState();
            }
        });

        function stopSpeaking() {
            speechService.stopSpeaking();
        }

        function speak(text, lang, speakerId = null) {
            speechService.speak(text, lang, speakerId);
        }

        // Phase 3: bind events and start the runtime in the original order.
        initializeSettingsControls();

        speechStateReady = true;
        updateSpeechIconState();

        bindGlobalShortcut();

        bindPanelActionControls();

        function getFullscreenController() {
            if (fullscreenController) return fullscreenController;
            fullscreenController = createFullscreenController({
                ui: {
                    fullscreenOverlay,
                    fullscreenTitleEl,
                    fullscreenClose,
                    fullscreenSourceLangSelect,
                    fullscreenTargetLangSelect,
                    fullscreenSourceLangCurrent,
                    fullscreenTargetLangCurrent,
                    fullscreenSourceLangSearch,
                    fullscreenTargetLangSearch,
                    fullscreenSourceLangGrid,
                    fullscreenTargetLangGrid,
                    fullscreenSourceLangPanel,
                    fullscreenTargetLangPanel,
                    fullscreenSourceLabel,
                    fullscreenTargetLabel,
                    fullscreenSwap,
                    fullscreenSource,
                    fullscreenTarget,
                    fullscreenLoadingOverlay,
                    fullscreenLoadingTitle,
                    fullscreenSourceCopy,
                    fullscreenSourceSpeak,
                    fullscreenTargetCopy,
                    fullscreenTargetSpeak,
                    fullscreenToggle
                },
                panelUi: { sourceLangSelect, targetLangSelect },
                languageApi: {
                    getBrowserLanguage: () => browserLang,
                    getDefaultTargetLanguage: () => defaultTargetLang,
                    getOverlayLabels: () => overlayLabels,
                    getLanguageNames: () => langNames,
                    getLanguageLabel,
                    buildSourceLanguageOptionsHtml,
                    buildTargetLanguageOptions,
                    resolveTargetLanguageValue,
                    resolveSourceSpeechLanguage,
                    resolveTargetSpeechLanguage,
                    ensureSelectValue,
                    getLoaderTitleByMode
                },
                translationApi,
                speechApi: {
                    stopSpeaking,
                    speak,
                    getSpeechState: () => ({
                        playing: speechPlaying,
                        speakerId: currentSpeakerId
                    })
                },
                layoutApi: {
                    lockPageScrollForFullscreen,
                    unlockPageScrollForFullscreen,
                    resetFullscreenTextareaResize,
                    syncFullscreenTextareaHeights,
                    markFullscreenResizeStart
                },
                selectionApi: {
                    hideSelectionBubble,
                    hideBubbleCloseMenu,
                    scheduleSelectionBubbleUpdate
                },
                languagePanelsApi: {
                    hideLanguagePanels,
                    renderLanguageGrid,
                    bindFullscreenLanguageSearchControls,
                    bindFullscreenPanelTriggers
                },
                themeApi: {
                    refreshLanguagePanelTheme,
                    setButtonIconStroke,
                    copyFeedbackStroke: COPY_FEEDBACK_STROKE,
                    applyIconThemeColors
                },
                panelApi: { syncPanelLoadingTitle },
                runtimeState: {
                    getDetectedSourceLanguage: () => detectedSourceLang,
                    setDetectedSourceLanguage: language => {
                        detectedSourceLang = language;
                    },
                    getCurrentResolvedTargetLanguage: () => currentResolvedTargetLang,
                    setCurrentResolvedTargetLanguage: language => {
                        currentResolvedTargetLang = language;
                    },
                    getCurrentSelectedText: () => currentSelectedText,
                    getCurrentTranslatedText: () => currentTranslatedText
                },
                setLoaderState,
                writeSourceClipboardText: text => navigator.clipboard.writeText(text),
                writeTargetClipboardText: text => navigator.clipboard.writeText(text),
                setSourceFeedbackTimer: (callback, delay) => setTimeout(callback, delay),
                setTargetFeedbackTimer: (callback, delay) => setTimeout(callback, delay),
                setTranslationTimer: (callback, delay) => setTimeout(callback, delay),
                clearTimer: timerId => clearTimeout(timerId)
            });
            return fullscreenController;
        }
        const {
            bindFullscreenActionControls,
            bindFullscreenInputControls,
            bindFullscreenLanguageControls,
            bindFullscreenNavigationControls
        } = getFullscreenController();

        bindFullscreenActionControls();
        bindFullscreenInputControls();
        bindFullscreenLanguageControls();

        document.addEventListener('mousedown', (e) => {
            if (selectionBubble && !eventPathContains(e, selectionBubble)) {
                hideBubbleCloseMenu();
            }
            closeLanguagePanelsFromOutside(e, {
                beforeInline: () => closeThemePanelFromOutside(e)
            });
        });

        function updateInlinePanelsPosition() {
            updateLanguagePanelsPosition();
            positionThemePanel();
        }

        attachInlineLanguagePanel(sourceLangSelect);
        attachInlineLanguagePanel(targetLangSelect);
        attachInlineLanguagePanel(defaultTranslateLangSelect);
        attachInlineLanguagePanel(toolLanguageSelect);
        refreshLanguagePanelTheme();

        [translationBox, settingsPanel, fullscreenOverlay, fullscreenPanel].forEach((scrollEl) => {
            if (!scrollEl) return;
            scrollEl.addEventListener('scroll', () => {
                updateInlinePanelsPosition();
            }, { passive: true });
        });

        bindFullscreenNavigationControls();
        bindPanelNavigationControls();


        function isClickInProtectedOverlay(event) {
            return !!(
                isClickInInlineLanguagePanel(event) ||
                isClickInFullscreenLanguagePanel(event) ||
                (fullscreenOverlay && eventPathContains(event, fullscreenOverlay)) ||
                (selectionBubble && eventPathContains(event, selectionBubble)) ||
                (panelThemePanel && eventPathContains(event, panelThemePanel)) ||
                (panelThemeTrigger && eventPathContains(event, panelThemeTrigger))
            );
        }

        bindPanelOutsideControls(isClickInProtectedOverlay);

        bindSelectionEvents({
            onBeforeMouseUp: finishFullscreenTextareaResize
        });

        window.addEventListener('scroll', (e) => {
            if (isFullscreenOpen()) {
                return;
            }
            if (translationBox.style.display !== 'block' && e.target !== document && e.target !== document.documentElement) {
                return;
            }
            updateInlinePanelsPosition();
            scheduleSelectionBubbleUpdate(0);
        }, true);
        window.addEventListener('resize', () => {
            updateInlinePanelsPosition();
            syncFullscreenTextareaHeights();
            hideSelectionBubble();
            scheduleSelectionBubbleUpdate(40);
        });

        requestAnimationFrame(() => {
            utstUi.host.style.removeProperty('visibility');
            utstUi.host.style.removeProperty('pointer-events');
        });
    }

    if (document.body) {
        bootstrap();
    } else {
        window.addEventListener('DOMContentLoaded', bootstrap);
    }
}

runUltimateTextSelectionTranslator();
